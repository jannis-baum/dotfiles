#!/bin/zsh

if [[ -z "$1" ]]; then
    echo "usage: $0 process-name" >&2
    exit 1
fi

log_dir="$HOME/.cache/logs"
mkdir -p "$log_dir"

log_name="$log_dir/$1"
# only keep newest 16 log files
ls -1t "$log_name"* | tail -n +17 | xargs rm --

echo "$log_name""_$(date +%y-%m-%d_%H-%M-%S)"
